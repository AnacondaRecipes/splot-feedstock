{% set name = "splot" %}
{% set version = "1.1.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 6edcaee24cd417965c378c5bc87414d02806915950c138c1f6e40f1a1de5286b

build:
  number: 0
  skip: true  # [py<38]
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python
    - pip
    - setuptools
    - wheel
  run:
    - python
    - esda
    - geopandas >=0.9.0
    - giddy
    - libpysal
    # added min bound: ImportError: cannot import name 'inf' from 'scipy' (issue was
    # fixed upstream from 4.7.0 and above)
    - libpysal >=4.7  # [py>=310]
    - mapclassify
    - matplotlib >=3.3.3
    - numpy
    - packaging
    - seaborn >=0.11.0
    - spreg
    # ipywidgets required in readme
    - ipywidgets
  run_constrained:
    - fiona <1.9  # [py<310]
    # current available version of geopandas package doesn't support shapely>=2
    - shapely <2  # [py<310]
    # ImportError: cannot import name 'inf' from 'scipy'. cap scipy on osx as we only
    # only have libpysal >4.7.0
    - scipy <1.12  # [osx and x86_64 and py<310]

{% set tests_to_skip = "" %}

# NameError: name 'np' is not defined (spreg)
{% set tests_to_skip = tests_to_skip + " --deselect=tests/test_viz_esda_mpl.py::test_moran_scatterplot" %}  # [py>312]
{% set tests_to_skip = tests_to_skip + " --deselect=tests/test_viz_esda_mpl.py::test_moran_global_scatterplot" %}  # [py>312]
{% set tests_to_skip = tests_to_skip + " --deselect=tests/test_viz_esda_mpl.py::test_plot_moran" %}  # [py>312]
{% set tests_to_skip = tests_to_skip + " --deselect=tests/test_viz_esda_mpl.py::test_moran_bv_scatterplot" %}  # [py>312]
{% set tests_to_skip = tests_to_skip + " --deselect=tests/test_viz_esda_mpl.py::test_plot_moran_bv" %}  # [py>312]
{% set tests_to_skip = tests_to_skip + " --deselect=tests/test_viz_esda_mpl.py::test_moran_loc_scatterplot" %}  # [py>312]
{% set tests_to_skip = tests_to_skip + " --deselect=tests/test_viz_esda_mpl.py::test_plot_local_autocorrelation" %}  # [py>312]
{% set tests_to_skip = tests_to_skip + " --deselect=tests/test_viz_esda_mpl.py::test_moran_loc_bv_scatterplot" %}  # [py>312]
{% set tests_to_skip = tests_to_skip + " --deselect=tests/test_viz_esda_mpl.py::test_moran_facet" %}  # [py>312]

test:
  imports:
    - splot
    - splot.esda
    - splot.giddy
    - splot.libpysal
    - splot.mapping
  requires:
    - pip
    - pytest
    - nose
    # bokeh: AttributeError: unexpected attribute 'plot_width'
    - bokeh <3.0.0a0  # [py<312]
    - bokeh           # [py>=312]
  commands:
    - pip check
    - python -c "from importlib.metadata import version; assert(version('{{ name }}')=='{{ version }}')"
    - pytest {{ tests_to_skip }} -vv --pyargs splot

about:
  home: https://pysal.org
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE.txt
  summary: Lightweight plotting and mapping to facilitate spatial analysis with PySAL
  description: |
    splot connects spatial analysis done in PySAL to different popular visualization
    toolkits like matplotlib. The splot package allows you to create both static plots
    ready for publication and interactive visualizations for quick iteration and
    spatial data exploration. The primary goal of splot is to enable you to visualize
    popular PySAL objects and gives you different views on your spatial analysis workflow.
  doc_url: https://splot.readthedocs.io
  dev_url: https://github.com/pysal/splot

extra:
  recipe-maintainers:
    - slumnitz
    - knaaptime
